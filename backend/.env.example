# Option A: fal.ai (cloud, no GPU needed)
# Get key at https://fal.ai/dashboard/keys
# FAL_KEY=your-key-id:your-secret

# Option B: Local Hugging Face (no key, needs CUDA GPU)
# Leave FAL_KEY empty and run: pip install -r requirements-local.txt
# USE_LOCAL_MODEL=1
